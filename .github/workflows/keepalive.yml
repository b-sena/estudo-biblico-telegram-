name: Keepalive

on:
  schedule:
    - cron: "0 12 1 * *"  # Todo dia 1 de cada mês
  workflow_dispatch:

jobs:
  keepalive:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Keepalive
        run: |
          git config user.email "action@github.com"
          git config user.name "GitHub Action"
          git commit --allow-empty -m "chore: keepalive"
          git push